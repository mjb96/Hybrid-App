{
  "name": "hybrid-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs",
    "typecheck": "tsc -p jsconfig.json"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
